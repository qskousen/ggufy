name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-latest, windows-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Build for Linux x86-64
        if: matrix.os == 'ubuntu-24.04'
        run: |
          zig build --release=small
          mkdir -p dist/linux
          cp zig-out/bin/ggufy zig-out/bin/ggufy-gui dist/linux/
          cd dist/linux
          zip ../ggufy-linux-x86_64.zip ggufy ggufy-gui
          cd ../..

      - name: Build for Windows x86-64
        if: matrix.os == 'windows-latest'
        run: |
          zig build --release=small -Dtarget=x86_64-windows
          mkdir -p dist/windows
          cp zig-out/bin/ggufy.exe zig-out/bin/ggufy-gui.exe dist/windows/
          Compress-Archive -Path dist/windows/ggufy.exe,dist/windows/ggufy-gui.exe -DestinationPath dist/ggufy-windows-x86_64.zip

      - name: Build for macOS x86-64
        if: matrix.os == 'macos-latest'
        run: |
          zig build --release=small -Dtarget=x86_64-macos --sysroot "$(xcrun --sdk macosx --show-sdk-path)"
          mkdir -p dist/macos-x86_64
          cp zig-out/bin/ggufy zig-out/bin/ggufy-gui dist/macos-x86_64/
          cd dist/macos-x86_64
          zip ../ggufy-macos-x86_64.zip ggufy ggufy-gui
          cd ../..

      - name: Build for macOS aarch64
        if: matrix.os == 'macos-latest'
        run: |
          zig build --release=small -Dtarget=aarch64-macos --sysroot "$(xcrun --sdk macosx --show-sdk-path)"
          mkdir -p dist/macos-aarch64
          cp zig-out/bin/ggufy zig-out/bin/ggufy-gui dist/macos-aarch64/
          cd dist/macos-aarch64
          zip ../ggufy-macos-aarch64.zip ggufy ggufy-gui
          cd ../..

      # -------------------- Upload artifacts --------------------
      - name: Upload artifacts (Linux)
        if: matrix.os == 'ubuntu-24.04'
        uses: actions/upload-artifact@v4
        with:
          name: linux-artifacts
          path: dist/ggufy-linux-x86_64.zip

      - name: Upload artifacts (Windows)
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: dist/ggufy-windows-x86_64.zip

      - name: Upload artifacts (macOS)
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: macos-artifacts
          path: |
            dist/ggufy-macos-x86_64.zip
            dist/ggufy-macos-aarch64.zip

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/linux-artifacts/ggufy-linux-x86_64.zip
            dist/windows-artifacts/ggufy-windows-x86_64.zip
            dist/macos-artifacts/ggufy-macos-x86_64.zip
            dist/macos-artifacts/ggufy-macos-aarch64.zip
          draft: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}